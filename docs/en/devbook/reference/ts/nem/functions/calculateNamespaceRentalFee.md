# Function: calculateNamespaceRentalFee()

```ts
function calculateNamespaceRentalFee(isRoot): bigint
```

Calculates the minimum required namespace rental fee.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `isRoot` | `boolean` | \c true if the fee should be calculated for a root namespace. |

## Returns

`bigint`

Rental fee.
