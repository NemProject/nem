name: increment
in: query
required: true
description: |
  Step between consecutive block heights. Must be greater than 0.
  The total number of returned data points must not exceed 1000.
schema:
  type: integer
  format: int32
  minimum: 1
examples:
  default:
    value: 1
